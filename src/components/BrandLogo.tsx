import Brand from "../assets/brand.svg";

const BrandLogo = () => {
  return (
    <span onClick={() => {}} className="hover:cursor-pointer">
      <img className="w-32" src={Brand} alt="" />
    </span>
  );
};

export default BrandLogo;
